import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.studybuddy.app",
  appName: "رفيق المذاكرة",
  webDir: "www",
  bundledWebRuntime: false
};

export default config;
